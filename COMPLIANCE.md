# 合规审查 · hanzi.fun（v0.2 验证版）

审查日期：2026-09-09（09-09 晚更新：开放项 1/2 已解决）　审查方式：**逐条对照线上代码与请求**，不是抄模板。
原则：文档写到的每一句，都必须能在代码里指出对应实现；代码里做的每一件数据的事，都必须在文档里说。

---

## 一、合规审查七问（按实际行为回答）

### 1. 产品碰了什么数据？（数据清单）

| 数据 | 采集方式 | 去向 | 依据（代码位置） |
|---|---|---|---|
| 页面 URL / referrer | Plausible 自动 | 自建 Plausible | `index.html:26` |
| 国家（由 IP 推导，**IP 本身不存储**） | Plausible 边缘推导 | 同上 | Plausible 默认行为 |
| 浏览器 / OS / 设备类型 | User-Agent 解析 | 同上 | 同上 |
| 游玩事件（非个人）：`session_start{fresh}`、`radical_pick{id}`、`combine_success{out,id,tier}`、`combine_fail{parts,level,streak}`、`hint_auto{target}`、`share_copy{id}}` | `track()` | 同上 | `src/game.js:132/141/167/179/210/278` |
| 游玩进度 + 事件日志 | localStorage | **只在本机**，永不上传 | `src/game.js:5-6, 28-42` |
| 剪贴板写入 | 用户主动点 Copy | 本机剪贴板 | `src/game.js:206-212` |

**不碰的数据**：姓名/邮箱/密码（无账号）、Cookie（全站零 Cookie）、支付信息、UGC、位置精度高于国家级别的一切信息。

### 2. 数据发给了谁？（第三方链路）

**全站第三方端点恰好一个**：`https://plausible.shipsolo.io`（脚本 + 事件 API）。
字体（woff2）、样式、图片、JS 全部同源。无广告网络、无 CDN 嵌入脚本、无社交 SDK。

自建 Plausible = **自己是数据处理者，也是数据控制者**，没有外部分析公司。

### 3. 数据存在哪里、存多久？

| 位置 | 内容 | 保留 |
|---|---|---|
| 访客浏览器 localStorage | 进度 + 最近 1000 条事件日志 | 用户清掉为止（Reset 按钮即擦除） |
| 自建 Plausible 服务器（shipsolo） | 匿名聚合统计 | ⚠️ **待定，见开放项 #2** |

### 4. 用户有什么权利？（GDPR / CCPA）

- **擦除权**：游戏内即可自服务——`S` → Reset progress（localStorage）；分析记录不含标识符，无法关联到个人，等同已擦除。
- **访问权/可携带权**：因为我们不收集任何标识符，**无法定位到个人**，GDPR 下的访问请求事实上无对象可返回；privacy 页如实写了这一点。
- **CCPA "不出售/不共享"**：无广告、无数据出售、无 cross-context behavioral advertising，privacy 页如实声明。
- **未成年人**：COPPA——不针对性收集 13 岁以下儿童数据（实际上对任何人都不收集个人数据）。

### 5. AI 生成的内容谁负责？

**本站没有 AI 生成内容**：兽图为手写 SVG + 程序化几何，文案为人工撰写，无 LLM 参与运行时服务。无需 AI 免责声明，**也不装模作样放一个**（文档与实际不一致比没有文档更糟）。

### 6. 内容过滤做了几层？

无 UGC、无评论、无聊天——**没有需要过滤的用户内容，N/A**。站点本身内容（汉字合成）为全年龄向。

### 7. 免责声明 / 权益描述 / 隐私卖点

- **字源简化免责**：宪法级红线只有一条——**不能教错**。SPEC 里已标注 出/晶/因/困 等字源有争议的字不进合成表、不写"古人认为"。privacy 页尾部加了一句 "simplified for fun, not a language-learning reference"。
- **隐私卖点**：privacy 页如实描述"cookieless、无账号、无广告"，**这是真的**，所以可以当卖点写。
- ❌ 没有写的：Terms of Service（无注册无交易，验证期不需要；等加排行榜/账号时必须补）。

---

## 二、已上线 vs 待办

**本轮已做（待重传 zip 生效）**：
- `privacy.html` 上线（`/privacy`，noindex），footer 挂链接
- `gallery / weight / og` 三个内部页加 `noindex`（不该被搜索引擎收录）
- `sitemap.xml` 加 /privacy

**开放项（按风险排序）**：

| # | 事项 | 风险 | 负责人 |
|---|---|---|---|
| 1 | ~~联系方式~~ **已解决（09-09）**：`support@hanzi.fun` 已配置自动转发并实测可用，privacy 页已更新 | ✅ | — |
| 2 | ~~服务器位置~~ **已确认（09-09）**：Plausible 部署在 Cloudflare（美西，San Francisco）。因不收集标识符/IP 不落盘，无跨境传输个人数据问题；privacy 页已写明 hosted in the United States | ✅ | — |
| 3 | **将来加排行榜/账号时**：才是真 GDPR 时刻（DPA、ToS、年龄门槛、请求处理流程）。验证期不做 | 届时高 | 届时再评 |
| 4 | CF 注入的 robots `ai-train=no`（GPTBot 等 Disallow）：不是法律问题，是**流量策略问题**——要不要让 AI 搜索引用内容页 | — | 待定 |

---

## 三、一句话结论

**这个站的真实风险面非常小**（无 PII、无账号、无 UGC、无 AI 内容、单一自建分析端点），
合规负担的主体是"文档如实、联系方式真实、服务器说得清"三件事，都已处理或列出。
真正会触发重合规的是**加账号/排行榜**——在那之前不需要买任何合规服务。
